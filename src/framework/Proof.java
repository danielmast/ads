package framework;

/**
 * Defines how a query response looks like. Consists of both the content and the associated proof.
 */
public abstract class Proof<A extends ADS> {

    public Proof(A ADS, int index) {
    }
}
